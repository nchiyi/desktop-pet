{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["pet", "chat", "settings", "guide"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
